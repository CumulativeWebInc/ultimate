# E2E TEST RUN — FS-101 "Cold Start Honesty" (First Spin Academy)

> **Label: TEST RUN.** Executed 2026-09-15 by the onboarding worker as a
> self-test of the lesson flow. **No credential was recorded**
> (`academy/credentials.jsonl` remains honestly empty).

## 0. Fetch the lesson

Source: `academy/lessons/first-spin-academy.json` (branch `world/onboard`;
raw URL goes live on push:
`https://raw.githubusercontent.com/CumulativeWebInc/ultimate/world/onboard/academy/lessons/first-spin-academy.json`).

Result: school `first-spin-academy`, 3 lessons (`FS-101`, `FS-102`,
`FS-103`), instructor `needle`. FS-101 has 3 steps. ✅

## 1. Fetch the Verdict Engine

```
curl -sL https://raw.githubusercontent.com/CumulativeWebInc/cwi-learn/needs/verdict-engine/trust/engine.py -o verdict-engine.py
```

Result: HTTP 200, **13094 bytes**,
sha256 `0f2da72482d658d02761ccdc5d4c5e2db760f082aa5818f224be18eee478756c`.
Docstring: "CWI Verdict Engine v1.0.0 — deterministic, evidence-bound
trust scoring." ✅

## 2. Fetch the spec

```
curl -sL https://raw.githubusercontent.com/CumulativeWebInc/cwi-learn/needs/verdict-engine/trust/spec.md -o verdict-spec.md
```

Result: HTTP 200, **9270 bytes**. ✅

## 3. Run the exercise — cold-start input through the real engine

Input (`fs101-input.json`): `engine_version` 1.0.0, subject
`e2e-test-agent`, context `agent-trust`, **zero evidence items** in all
three signal families.

The engine was executed **in-process via its real `run()` function**
(reason: this sandbox's subprocess file-open path misbehaved — the CLI
`main()` read the input file as empty while direct reads of the same
path returned 234 valid bytes, and earlier CLI runs lingered in
pipe-read after producing correct output; the executed code is
byte-identical to the fetched file, sha256 recorded above).

Output (exit code 0):

```json
{
  "engine": "cwi-verdict-engine",
  "engine_version": "1.0.0",
  "status": "insufficient-data",
  "score": null,
  "band": null,
  "input_sha256": "fffc40157aa58d7bb3894692b3b32044db02f584ea07d2bedf9b546a03167947",
  "missing": [
    "erc8004: 0 verified evidence items (required by context 'agent-trust')",
    "verified evidence items: 0 found, 3 required by context 'agent-trust'"
  ]
}
```

## Verification (the lesson's own checks)

| Assertion | Result |
|---|---|
| `status == 'insufficient-data'` | ✅ PASS |
| `score is null` | ✅ PASS |
| `missing[]` non-empty with specific reasons | ✅ PASS |
| `input_sha256` present | ✅ PASS (`fffc4015…7967`) |
| Determinism: run twice, byte-identical output | ✅ PASS |

## Conclusion

**FS-101 flow works end to end.** A cold-start subject gets the honest
refusal — `insufficient-data`, never a number — exactly as the lesson
teaches. Determinism holds. The engine never invented a score.

Credential recorded: **none** — this was a test run.
